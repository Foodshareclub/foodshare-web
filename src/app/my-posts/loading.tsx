import { MyPostsSkeleton } from './MyPostsSkeleton';

/**
 * Loading UI for My Posts page
 * Automatically shown by Next.js during page transitions
 */
export default function Loading() {
  return <MyPostsSkeleton />;
}
