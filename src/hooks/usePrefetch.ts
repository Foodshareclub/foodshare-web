/**
 * Prefetch common routes after initial page load
 * Note: In Next.js, route prefetching is automatic via the Link component
 * This hook is kept for compatibility but does nothing in Next.js
 */
export function usePrefetch() {
  // No-op in Next.js - prefetching is handled automatically
  return;
}
