# API Proxy Layer (`src/api/`)

External API client wrappers for third-party services. These are used by server actions and data layer functions to communicate with external APIs (not Supabase).

**Owner**: Backend team
**Runtime**: Server-only (Node.js)
