# API Client Library (`src/lib/api/`)

Shared API utilities: HTTP client, retry logic, circuit breaker, rate limiting. Used by both the API proxy layer and server actions.

**Owner**: Platform team
**Runtime**: Server-only (Node.js)
