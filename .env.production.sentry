# Sentry Production Environment Configuration
# NOTE: values below are placeholders — real secrets live in CI/VPS env.
NEXT_PUBLIC_SENTRY_DSN=https://<key>@o4512048058859520.ingest.us.sentry.io/4512048227876864
SENTRY_DSN=https://<key>@o4512048058859520.ingest.us.sentry.io/4512048227876864
SENTRY_ENVIRONMENT=production
# Set per-deploy in CI (github.sha); enables suspect-commits → issues/PRs
SENTRY_RELEASE=
NEXT_PUBLIC_SENTRY_RELEASE=
SENTRY_ORG=organicnz-7v
SENTRY_PROJECT=foodshare-web
# Enable Sentry when DSN is configured
SENTRY_DEBUG=false